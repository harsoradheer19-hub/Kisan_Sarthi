name: kisan_sarthi
description: "Kisan Sarthi - Har Kisan Ka Smart Saathi (Agricultural Advisory System)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  provider: ^6.0.5
  http: ^1.1.0
  shared_preferences: ^2.2.0
  supabase_flutter: ^2.5.0
  video_player: ^2.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/kisansarthi_loading.mp4

